using Microsoft.Xna.Framework;

namespace MyApp
{
    /// <summary>
    /// material index list
    /// </summary>
    public enum MaterialType : byte
    {
        Empty = 0,
        Sand = 1,
        Water = 2,
    }
}